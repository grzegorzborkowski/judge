version: "2"
services:
 db:
  build:
   context: judge-db
   dockerfile: Dockerfile
  ports:
   - "5432:5432"
 go:
  build:
   context: judge-external-runner/go
   dockerfile: Dockerfile
  ports:
   - "8123:8123"
 web:
  build:
   context: judge-frontend
   dockerfile: Dockerfile
  ports:
   - "3008:3008"

